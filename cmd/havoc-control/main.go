// havoc-control is the HTTP API + CLI for the Havoc chaos engineering
// platform. It validates experiments, enforces safety guardrails, and
// publishes commands to Kafka.
//
// Phase 1 scaffold — the API and CLI land in Phase 2.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "havoc-control: not yet implemented (arrives in Phase 2)")
	os.Exit(1)
}
